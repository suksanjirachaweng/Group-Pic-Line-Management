{
  "name": "grouppic-photo-server",
  "version": "1.0.0",
  "description": "Self-hosted photo storage server for the Group Pic Registration app — runs on the admin's own PC instead of Vercel Blob.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "check-disk-space": "^3.4.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "onnxruntime-node": "1.19.2",
    "sharp": "^0.33.5"
  }
}
